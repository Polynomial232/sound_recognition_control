name: Sound Recognition Control Python Application

on: [push]

jobs:
  Run-Ubuntu:
    runs-on: ubuntu-latest
    steps:
    - name: Create file
      run: |
        echo Hello World
        echo Hello World > test.txt
    - name: Check file
      run: |
        cat test.txt
      
